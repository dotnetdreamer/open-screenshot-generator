{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: save/open dialogs, writing exported files, opening external links in the system browser, and calling the free AI providers (Pollinations cloud, plus Ollama / LM Studio on localhost) without CORS limits.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "fs:allow-write-file",
    "fs:allow-read-file",
    "opener:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://text.pollinations.ai/*" },
        { "url": "http://localhost:*" },
        { "url": "http://127.0.0.1:*" }
      ]
    }
  ]
}
